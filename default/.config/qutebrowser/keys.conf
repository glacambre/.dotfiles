# vim: ft=conf
#
# In this config file, qutebrowser's key bindings are configured.
# The format looks like this:
#
# [keymode]
#
# command
#   keychain
#   keychain2
#   ...
#
# All blank lines and lines starting with '#' are ignored.
# Inline-comments are not permitted.
#
# keymode is a comma separated list of modes in which the key binding should be
# active. If keymode starts with !, the key binding is active in all modes
# except the listed modes.
#
# For special keys (can't be part of a keychain), enclose them in `<`...`>`.
# For modifiers, you can use either `-` or `+` as delimiters, and these names:
#
#  * Control: `Control`, `Ctrl`
#  * Meta:    `Meta`, `Windows`, `Mod4`
#  * Alt:     `Alt`, `Mod1`
#  * Shift:   `Shift`
#
# For simple keys (no `<>`-signs), a capital letter means the key is pressed
# with Shift. For special keys (with `<>`-signs), you need to explicitly add
# `Shift-` to match a key pressed with shift.
#
# Note that default keybindings are always bound, and need to be explicitly
# unbound if you wish to remove them:
#
# <unbound>
#   keychain
#   keychain2
#   ...

[!normal]

leave-mode
    <escape>
    <ctrl-[>

[normal]
# Keybindings for normal mode.

clear-keychain ;; search ;; fullscreen --leave
    <escape>
    <ctrl-[>

set-cmd-text -s :open
    o

set-cmd-text :open {url:pretty}
    O

set-cmd-text -s :open -t
    t

set-cmd-text :open -t -i {url:pretty}
    T

set-cmd-text -s :open -w
    w

set-cmd-text :open -w {url:pretty}
    W

set-cmd-text /
    /

set-cmd-text ?
    ?

set-cmd-text :
    :

open -w
    <ctrl-n>

tab-close
    d

tab-move -
    <alt-h>

tab-move +
    <alt-l>

tab-next
    L

tab-prev
    H

reload
    r

reload -f
    R

back
    J

forward
    K

hint all
    F

hint all tab
    f

hint links yank
    Y

hint links images download
    S

run-with-count 3 scroll left
    h

run-with-count 3 scroll down
    j

run-with-count 3 scroll up
    k

run-with-count 3 scroll right
    l

undo
    u

scroll-perc 0
    gg

scroll-perc
    G

search-next
    n

search-prev
    N

enter-mode insert
    i

enter-mode caret
    v

enter-mode set_mark
    `

enter-mode jump_mark
    '

yank pretty-url
    y

open -- {clipboard}
    p

open -t -- {clipboard}
    P

quickmark-save
    a

set-cmd-text -s :quickmark-load
    go

set-cmd-text -s :quickmark-load -t
    gn

bookmark-add
    A

set-cmd-text -s :bookmark-load
    gb

set-cmd-text -s :bookmark-load -t
    gB

zoom-out
    zo

zoom-in
    zi

zoom
    zz

navigate prev
    gh

navigate next
    gl

navigate prev -t
    gH

navigate next -t
    gL

download
    s

view-source
    gs

tab-focus last
    <ctrt-o>

enter-mode passthrough
    <ctrl-v>

scroll-page 0 1
    <ctrl-f>

scroll-page 0 -1
    <ctrl-b>

scroll-page 0 0.5
    <ctrl-d>

scroll-page 0 -0.5
    <ctrl-u>

tab-focus 1
    <alt-1>
    g0
    g^

tab-focus 2
    <alt-2>

tab-focus 3
    <alt-3>

tab-focus 4
    <alt-4>

tab-focus 5
    <alt-5>

tab-focus 6
    <alt-6>

tab-focus 7
    <alt-7>

tab-focus 8
    <alt-8>

tab-focus 9
    <alt-9>

tab-focus 10
    <alt-0>

tab-focus -1
    g$

print
    <ctrl-alt-p>

follow-selected
    <return>
    <ctrl-m>
    <ctrl-j>
    <shift-return>
    <enter>
    <shift-enter>

follow-selected -t
    <ctrl-return>
    <ctrl-enter>

repeat-command
    .

tab-pin
    <ctrl-p>

record-macro
    q

run-macro
    @

set-cmd-text -s :open -b
    xo

set-cmd-text :open -b -i {url:pretty}
    xO

open -t
    ga
    <ctrl-t>

tab-close -o
    D

tab-only
    co

tab-clone
    gC

back -w
    wh

forward -w
    wl

fullscreen
    <f11>

hint all window
    wf

hint all tab-bg
    ;b

hint all tab-fg
    ;f

hint all hover
    ;h

hint images
    ;i

hint images tab
    ;I

hint links fill :open {hint-url}
    ;o

hint links fill :open -t -i {hint-url}
    ;O

hint links yank-primary
    ;Y

hint --rapid links tab-bg
    ;r

hint --rapid links window
    ;R

hint links download
    ;d

hint inputs
    ;t

yank
    xxxxxxa

yank -s
    xxxxxxb

yank title
    xxxxxxc

yank title -s
    xxxxxxd

yank domain
    xxxxxxe

yank domain -s
    xxxxxxf

yank pretty-url -s
    xxxxxxg

open -- {primary}
    xxxxxxs

open -t -- {primary}
    xxxxxxt

open -w -- {clipboard}
    xxxxxxh

open -w -- {primary}
    xxxxxxi

set-cmd-text -s :quickmark-load -w
    xxxxxxj

set-cmd-text -s :bookmark-load -w
    xxxxxxk

save
    xxxxxxl

set-cmd-text -s :set
    xxxxxxm

set-cmd-text -s :set -t
    xxxxxxn

set-cmd-text -s :bind
    xxxxxxo

navigate up
    gu

navigate up -t
    gU

navigate increment
    <ctrl-a>

navigate decrement
    <ctrl-x>

inspector
    wi

download-cancel
    xxxxxxu

download-clear
    cd

set-cmd-text -s :buffer
    gt

quit
    <ctrl-q>
    ZQ

wq
    ZZ

home
    <ctrl-h>

stop
    <ctrl-s>

open qute://settings
    xxxxxxp

back -t
    xxxxxxq

forward -t
    xxxxxxr

tab-move
    gm

[insert]
# Keybindings for insert mode.
# Since normal keypresses are passed through, only special keys are
# supported in this mode.
# Useful hidden commands to map in this section:
#  * `open-editor`: Open a texteditor with the focused field.
#  * `paste-primary`: Paste primary selection at cursor position.

open-editor
    <ctrl-i>

insert-text {primary}
    <ctrl-y>

insert-text {selection}
    <ctrl-shift-y>

fake-key <Left>
    <ctrl-h>

fake-key <Down>
    <ctrl-j>

fake-key <Up>
    <ctrl-k>

fake-key <Right>
    <ctrl-l>

fake-key <Home>
    <ctrl-a>

fake-key <Shift-Home> ;; fake-key <Delete>
    <ctrl-u>

fake-key <End>
    <ctrl-e>

# fake-key <Shift-End> ;; fake-key <Delete>
#     <ctrl-k>

fake-key <Ctrl-Left>
    <ctrl-b>

fake-key <Ctrl-Shift-Left> ;; fake-key <Delete>
    <ctrl-w>

fake-key <Ctrl-Right> ;; fake-key <Right>
    <ctrl-f>

fake-key <Ctrl-Shift-Right> ;; fake-key <Delete>
    <ctrl-d>

[hint]
# Keybindings for hint mode.
# Since normal keypresses are passed through, only special keys are
# supported in this mode.
# Useful hidden commands to map in this section:
#  * `follow-hint`: Follow the currently selected hint.

follow-hint
    <return>
    <ctrl-m>
    <ctrl-j>
    <shift-return>
    <enter>
    <shift-enter>

hint --rapid links tab-bg
    <ctrl-r>

hint links
    <ctrl-f>

hint all tab-bg
    <ctrl-b>

[command]
# Keybindings for command mode.
# Since normal keypresses are passed through, only special keys are
# supported in this mode.
# Useful hidden commands to map in this section:
#  * `command-history-prev`: Switch to previous command in history.
#  * `command-history-next`: Switch to next command in history.
#  * `completion-item-focus`: Select another item in completion.
#  * `command-accept`: Execute the command currently in the commandline.

command-history-prev
    <up>

command-history-next
    <down>

completion-item-focus prev
    <shift-tab>

completion-item-focus next
    <tab>

completion-item-focus next-category
    <ctrl-tab>

completion-item-focus prev-category
    <ctrl-shift-tab>

completion-item-del
    <ctrl-d>

command-accept
    <return>
    <ctrl-m>
    <ctrl-j>
    <shift-return>
    <enter>
    <shift-enter>

[prompt]
# Keybindings for prompts in the status line.
# You can bind normal keys in this mode, but they will be only active
# when a yes/no-prompt is asked. For other prompt modes, you can only
# bind special keys.
# Useful hidden commands to map in this section:
#  * `prompt-accept`: Confirm the entered value.
#  * `prompt-accept yes`: Answer yes to a yes/no question.
#  * `prompt-accept no`: Answer no to a yes/no question.

prompt-accept
    <return>
    <ctrl-m>
    <ctrl-j>
    <shift-return>
    <enter>
    <shift-enter>

prompt-accept yes
    y

prompt-accept no
    n

prompt-open-download
    <ctrl-x>

prompt-item-focus prev
    <shift-tab>
    <up>

prompt-item-focus next
    <tab>
    <down>

[command,prompt]

rl-backward-char
    <ctrl-h>

rl-forward-char
    <ctrl-l>

rl-backward-word
    <ctrl-b>

rl-forward-word
    <ctrl-f>

rl-beginning-of-line
    <ctrl-a>

rl-end-of-line
    <ctrl-e>

rl-unix-line-discard
    <ctrl-u>

rl-kill-line
    <ctrl-k>

rl-kill-word
    <ctrl-d>

rl-backward-kill-word
    <ctrl-w>

rl-yank
    <ctrl-y>

rl-delete-char
    <ctrl-?>

[caret]

toggle-selection
    v
    <space>

drop-selection
    <ctrl-space>

enter-mode normal
    c

move-to-next-line
    xxxxxxz

move-to-prev-line
    xxxxxxy

move-to-next-char
    xxxxxxx

move-to-prev-char
    xxxxxxw

move-to-end-of-word
    e

move-to-next-word
    w

move-to-prev-word
    b

move-to-start-of-next-block
    ]

move-to-start-of-prev-block
    [

move-to-end-of-next-block
    }

move-to-end-of-prev-block
    {

move-to-start-of-line
    0

move-to-end-of-line
    $

move-to-start-of-document
    gg

move-to-end-of-document
    G

yank selection -s
    Y

yank selection
    y
    <return>
    <ctrl-m>
    <ctrl-j>
    <shift-return>
    <enter>
    <shift-enter>

scroll left
    h

scroll down
    j

scroll up
    k

scroll right
    l

